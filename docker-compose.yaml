version: '3.8'
services:
  gatee-api:
    image: ${CI_REGISTRY}/gatee-api:latest
    container_name: gatee-api
    ports:
      - "8080:8080"
    environment:
      - ENV=prod, common
    networks:
      - gatee-net

networks:
  gatee-net:
    driver: bridge
