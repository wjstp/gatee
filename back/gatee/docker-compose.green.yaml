services:
  gatee-api-green:
    image: ${CI_REGISTRY}/gatee-api:green
    container_name: gatee-api-green
    ports:
      - "8081:8080"
    environment:
      - ENV=prod
    env_file:
      - devops/.env
    networks:
      - gatee-net

networks:
  gatee-net:
    external: true
